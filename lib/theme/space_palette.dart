import 'package:flutter/material.dart';

/// Open grassland under a twilight night sky — navy → violet → rose → amber.
abstract final class SpacePalette {
  static const skyZenith = Color(0xFF010208);
  static const skyTop = Color(0xFF040816);
  static const skyMid = Color(0xFF090C22);
  static const skyViolet = Color(0xFF161028);
  static const skyRose = Color(0xFF2A1C2A);
  static const skyLow = Color(0xFF342420);
  static const skyHorizon = Color(0xFF543424);

  /// Soft milky wash — cool against the warm horizon.
  static const milkyCore = Color(0xFFEDE8FF);
  static const milkySoft = Color(0xFFB8A8E0);
  static const milkyBlue = Color(0xFFA0B8E8);
  static const milkyViolet = Color(0xFFA898D8);
  static const milkyWarm = Color(0xFFE8D0C0);
  static const milkyRose = Color(0xFFD8B0C0);

  /// Horizon glow — dusty rose into muted orange (blur only).
  static const hazeRose = Color(0xFF906050);
  static const hazeWarm = Color(0xFFA86840);
  static const hazeGold = Color(0xFFB87840);
  static const hazeGlow = Color(0xFFC88848);

  static const star = Color(0xFFF4F2FF);
  static const starWarm = Color(0xFFFFF0D4);
  static const starBlue = Color(0xFFD8E8FF);
  static const starGlow = Color(0x66FFFFFF);

  // Far dark silhouette / near very dark ochre earth.
  static const grassFar = Color(0xFF040508);
  static const grassMid = Color(0xFF0A0908);
  static const grassNear = Color(0xFF100E0A);
  static const grassLit = Color(0xFF16140E);
  static const grassWarm = Color(0xFF1A1610);
  static const grassTip = Color(0xFF282418);
  static const earth = Color(0xFF080604);

  static const comfort = Color(0xFFE8DCC8);
  static const shadeDeep = Color(0xFF020408);

  static const presence = Color(0xFFE8E0D0);
  static const presenceGlow = Color(0xFFF4ECDC);
  static const presenceCore = Color(0xFFFAF6F0);
  static const presenceInk = Color(0xFFC8C0B0);

  static const canvas = Color(0xFF040816);
}
